---
import Hero from "../components/Hero.astro";
import IntroSection from "../components/IntroSection.astro";
import ContentSection from "../components/ContentSection.astro";
import Layout from "../layouts/Layout.astro";
import BackgroundImage from "../components/BackgroundImage.astro";
import AccordionContainer from "../components/AccordionContainer.astro";
import AccordionItem from "../components/AccordionItem.astro";
---

<Layout>
  <div class="container mx-auto">
    <BackgroundImage />

    <main>
      <Hero title="Guide du Capitaine d'&eacute;quipe" />

      <IntroSection>
        <p>
          Alors... Vos membres d'équipe vous posent toutes sortes de questions et vous aimeriez avoir une seule page avec toutes les informations ? Nous avons créé une page exactement pour ça ! Bien que vous puissiez simplement diriger les participants vers l'une des autres pages d'aide, celle-ci est spécialement conçue pour vous.
          <br /><br />
          Si quelque chose n'est toujours pas clair, n'hésitez pas à nous contacter.<br />Cependant, si vous posez une question qui est clairement mentionnée sur le site web ou dans le document d'information, vous devrez un verre au comité 😉. (Oui, nous tenons les comptes 😜)
        </p>
      </IntroSection>

      <ContentSection title="Recent questions">
        <AccordionContainer>

        </AccordionContainer>
      </ContentSection>

      <ContentSection title="General information">
        <AccordionContainer>
        </AccordionContainer>
      </ContentSection>
    </main>
  </div>
</Layout>

<style>
  a {
    color: var(--color-primary);
    font-weight: bold;
  }
</style>
<script>
  // Accordion functionality
  document.addEventListener("DOMContentLoaded", () => {
    const accordionHeaders = document.querySelectorAll(".accordion-header");

    accordionHeaders.forEach((header) => {
      header.addEventListener("click", () => {
        const accordionItem = header.parentElement;

        if (!accordionItem) return;

        const isActive = accordionItem.classList.contains("active");

        // Close all accordion items
        document.querySelectorAll(".accordion-item").forEach((item) => {
          item.classList.remove("active");
        });

        // Open clicked item if it wasn't active
        if (!isActive) {
          accordionItem.classList.add("active");
        }
      });
    });
  });
</script>
